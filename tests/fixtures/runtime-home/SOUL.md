# Test Agent Behaviour Contract

Fixture only. Exists so the dashboard's runtime-context probe has something to find
without depending on the developer's real home directory.

The dashboard must report this as configured while withholding the text itself, so this
file deliberately contains no paths, names, or anything that would matter if it leaked.
